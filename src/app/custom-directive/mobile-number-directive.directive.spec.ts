import { MobileNumberDirectiveDirective } from './mobile-number-directive.directive';

describe('MobileNumberDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileNumberDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
